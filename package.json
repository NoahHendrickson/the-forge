{
  "name": "the-forge",
  "private": true,
  "workspaces": ["packages/*", "fixtures/*"],
  "scripts": {
    "test": "npm run typecheck -w @the-forge/vite && npm run test -w @the-forge/vite",
    "build": "npm run build -w @the-forge/vite"
  }
}
