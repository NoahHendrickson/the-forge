{
  "name": "the-forge",
  "private": true,
  "workspaces": ["packages/*", "fixtures/*"],
  "scripts": {
    "test": "npm run typecheck -w forge-mode && npm run test -w forge-mode",
    "build": "npm run build -w forge-mode"
  },
  "devDependencies": {
    "react": "^19.0.0",
    "react-dom": "^19.0.0"
  }
}
