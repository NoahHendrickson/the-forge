{
  "name": "the-forge",
  "private": true,
  "workspaces": ["packages/*", "fixtures/*"],
  "scripts": {
    "test": "npm run typecheck -w the-forge && npm run test -w the-forge",
    "build": "npm run build -w the-forge"
  }
}
